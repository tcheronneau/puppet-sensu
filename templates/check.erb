{
  "checks": {
    "<%= @name %>": {
	  "handlers":["default", "email"],
      "command": "<%= @command %> ",
      "interval": <%= @interval %>,
      "subscribers": [
        "<%= @subscribers %>"
      ]
    }
  }
}
